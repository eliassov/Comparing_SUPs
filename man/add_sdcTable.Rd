% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_sdcTable.R
\name{add_sdcTable}
\alias{add_sdcTable}
\title{Add sdcTable Suppression to SDC Object}
\usage{
add_sdcTable(
  filename,
  path = "merged",
  output = NULL,
  method = "SIMPLEHEURISTIC",
  pvalue = 5,
  use_external_primary = TRUE,
  time_limit = 3600,
  fatal_error = FALSE
)
}
\arguments{
\item{filename}{Character string; name of the file (excluding extension) containing the saved RDS object.}

\item{path}{Character string; directory where the RDS file is stored. Default is "merged".}

\item{output}{Character string specifying the output format, or NULL. Use "out_simple", "df_merged", "prob.microDat", or "resSIMPLE" to return objects instead of saving.}

\item{method}{Character string; the secondary suppression method to use (e.g., "SIMPLEHEURISTIC"). Default is "SIMPLEHEURISTIC".}

\item{pvalue}{Numeric; threshold for identifying unsafe cells if not using external primary suppressions. Default is 5.}

\item{use_external_primary}{Logical; if TRUE, uses existing primary suppressions from the precomputed "primary_gauss" column in `df_merged`. Default is TRUE.}

\item{time_limit}{Numeric; time limit in seconds for the solver. Default is 3600.}

\item{fatal_error}{Logical; if TRUE, manually registers a fatal error status to avoid re-running failing configurations. Default is FALSE.}
}
\value{
Modifies the saved RDS file with sdcTable suppression results, or returns a specific object depending on the `output` parameter.
}
\description{
Runs secondary cell suppression using the `sdcTable` package on a saved SDC object and adds the results.
}
